name: Aliyun Signin

on:
  schedule:
   # 每天国际时间 14:40 运行一次, 中国时间 22:40
    - cron: '40 14 * * *'
  workflow_dispatch:
jobs:
  signin:
    name: Aliyun Signin
    runs-on: ubuntu-latest
    steps:
      - uses: ImYrS/aliyun-auto-signin@main
        with:
          REFRESH_TOKENS: ${{ secrets.c5484d464a76493092d72994ae49b1c4 }}
          GP_TOKEN: ${{ secrets.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}}
          PUSH_TYPES: ''
          DO_NOT_REWARD: 'false'
          PUSHPLUS_TOKEN: ${{ secrets.5144188bf0a241d48d4d6a6889311647 }}
          PUSHPLUS_TOPIC: ${{ secrets.PUSHPLUS_TOPIC }}
